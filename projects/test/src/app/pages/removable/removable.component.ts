import { Component } from "@angular/core";

import { Logger } from "../../tools/logger";

@Component({
    selector: "app-removable-page",
    templateUrl: "./removable.component.html",
})
export class RemovablePageComponent extends Logger {}
